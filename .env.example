# SQLite (MVP 本機)。上線改 Postgres：
# DATABASE_URL="postgresql://user:password@localhost:5432/hfmon?schema=public"
DATABASE_URL="file:./dev.db"
HF_TOKEN=""
# Vercel Cron / GitHub Actions 共用密鑰，避免外部亂觸發收集
CRON_SECRET="change-me"
